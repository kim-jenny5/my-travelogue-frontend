// const placeReducer = (state = { placeName: null, tripId: null }, action) => {
// 	switch (action.type) {
// 		case "ADD_PLACE":
// 			return {
// 				tripId: action.data.trip.id,
// 				placeName: action.data.place_name
// 			};

// 		default:
// 			return state;
// 	}
// };

// export default placeReducer;
